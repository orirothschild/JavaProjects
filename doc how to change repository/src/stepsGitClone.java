/**
 * 
 */
/**
	 *1.we change to a new workspace:
	 *(file -> workspace->other->create requested folders)
	 *\n
	 *2.file -> import ->general ->existing java projects->browse to requested folder(check copy projects into,nested projects)->finish
	 *\n
	 *3.goto quickAcsess ->clone git. ->copy from gui git the http using the clone buttton on the gui and pasting to the accsess URL
	 *\n
	 *4.enter user password.+questions. checks if works with git push
	 *\n
	 *@author ori rothschild
	 */
public class stepsGitClone {

	}


